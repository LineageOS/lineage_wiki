{% assign device = site.data.devices[page.device] %}

## Pre-Install Information
Make sure you select the correct console revision. If you don't know your console revision, reboot into the bootloader.

The orientation of the large green "START" text:
* Left-facing = `odin_m0`
* Right-facing = `odin_m2`.
